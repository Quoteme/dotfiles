# Sudo
alias s='sudo'

# CD
alias cd..='cd ..'
alias ..='cd ..'
alias ...='cd ../..'

# RM
alias rm='rm -r'
alias rmf='rm -rf'

# CP
alias cp='cp -r'
alias cpp='rsync -PrlpE'
alias cpz='rsync -PrlpEz'

# LS
alias l='exa'
alias ls='exa -l'
alias ll='exa -l'
alias lt='exa -lT'

# MKDIR
alias mk='mkdir -p'
alias mkdir='mkdir -p'

# NT
alias nt='st &'

# less
alias less=$PAGER
alias zless=$PAGER

# sxiv
alias sxiv=sxiv-rifle
