# Sudo
alias s='sudo'

# CD
alias cd..='cd ..'
alias ..='cd ..'
alias ...='cd ../..'
alias ~='cd ~'

# RM
alias rm='rm -r'
alias rmf='rm -rf'

# CP
alias cp='cp -r'
alias cpp='rsync -PrlpE'
alias cpz='rsync -PrlpEz'

# LS
alias l='exa'
alias ls='exa -l'
alias ll='exa -l'
alias lt='exa -lT'

# MKDIR
alias mk='mkdir -p'
alias mkdir='mkdir -p'

# NT
alias nt='urxvt &'
alias st='st -f scientifica:pixelsize=14'

# Vifm
alias vifm='vifm ./ ./'

# Vim bindings
alias :q='exit'
alias vim='nvim'

# dev
alias h="hoogle --count=15"

# accessibility
alias onboard='.local/bin/resart_onboard.sh'

# maim is a better scrot
alias scrot=maim
